# Pytest config (plan §4.1)
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
addopts = -v
markers =
    network: opt-in tests that require real network access
