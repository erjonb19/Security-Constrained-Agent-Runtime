# Dependencies for the FastAPI service, planner, graph, and eval harness.
#
# PINNED deliberately. These are the exact versions the full test suite (349
# tests) and both eval suites were verified against. Unpinned, CI resolves the
# latest release on every run, so a breaking upstream change (fastapi, langgraph,
# sqlglot, openai) arrives as a mystery CI failure with no matching code change.
# Bump these on purpose, in a PR, with the suite green -- not by accident.

fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
duckdb==1.5.4
sqlglot==30.11.0
openai==2.43.0
pyyaml==6.0.3
langgraph==1.2.10
langgraph-checkpoint-sqlite==3.1.1
slowapi==0.1.10
python-dotenv==1.2.2

# Only needed when DATA_BACKEND=databricks. The Databricks path is VERIFIED
# end-to-end against a real workspace (Free Edition serverless SQL); it is left
# out of the default install because the local/Render deploy runs DuckDB and the
# connector pulls a large dependency tree.
# databricks-sql-connector==4.4.0
