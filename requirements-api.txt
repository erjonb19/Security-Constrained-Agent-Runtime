# Dependencies for the FastAPI service, planner, graph, and eval harness.
#
# PINNED deliberately. These are the exact versions the full test suite and
# both eval suites were verified against. Unpinned, CI resolves the
# latest release on every run, so a breaking upstream change (fastapi, langgraph,
# sqlglot, openai) arrives as a mystery CI failure with no matching code change.
# Bump these on purpose, in a PR, with the suite green -- not by accident.

fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
duckdb==1.5.4
sqlglot==30.11.0
openai==2.43.0
pyyaml==6.0.3
langgraph==1.2.10
langgraph-checkpoint-sqlite==3.1.1
slowapi==0.1.10
python-dotenv==1.2.2

# The Databricks path is VERIFIED end-to-end against a real workspace (Free
# Edition serverless SQL). Installed by default so the UI's per-query "Databricks
# cloud" selector works in production -- the connector must be present for the
# backend to be offered at all, even though DATA_BACKEND stays 'local'.
# analytics_query_tool.py imports it lazily and degrades to a clear
# "not configured" message if the credentials are absent, so installing it is
# safe even on deploys that never use it.
databricks-sql-connector==4.4.0
